<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title) %> | Fantasy 500</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="UTF-8">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <h1 class="logo">
        <a href="/" class="text-light">🏎 Fantasy 500</a>
      </h1>
      <div class="nav-container">
        <ul class="nav">
          <li class="nav-item"><%= link_to "🏆 Scoreboard", welcome_index_path %></li>
          <li class="nav-item"><%= link_to "👥 Teams", teams_path %></li>
          <li class="nav-item"><%= link_to "🏎 Drivers", drivers_path %></li>
        </ul>
      </div>
    </header>
    <main>
      <div class="flashes">
        <% flash.each do |message_type, message| %>
          <div class="toast toast-<%= message_type %>"><%= message %></div>
        <% end %>
      </div>
      <%= yield %>
    </main>
    <footer>
    <%= debug(params) if Rails.env.development? %>
    </footer>
  </body>
</html>
