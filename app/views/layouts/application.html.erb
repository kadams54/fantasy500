<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title) %> | Fantasy 500</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="UTF-8">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <h1 class="logo">
        <a href="/" class="text-light">🏎 Fantasy 500</a>
      </h1>
      <div class="nav-container">
        <ul class="nav" data-testid="nav">
          <% if logged_in? %>
            <li class="nav-item"><%= link_to "Dashboard", root_path %></li>
            <li class="nav-item"><%= link_to "Log Out", logout_path, method: :delete %></li>
          <% else %>
            <li class="nav-item"><%= link_to "Sign Up", signup_path %></li>
            <li class="nav-item"><%= link_to "Log In", login_path %></li>
          <% end %>
        </ul>
        <!--
        <ul class="nav-league">
          <li class="nav-item"><%= link_to "🏆 Scoreboard", welcome_index_path %></li>
          <li class="nav-item"><%= link_to "👥 Teams", teams_path %></li>
          <li class="nav-item"><%= link_to "🏎 Drivers", drivers_path %></li>
        </ul>
        -->
      </div>
    </header>
    <main>
    <div class="container">
      <div class="columns">
        <div class="column col-12">
          <div class="flashes" data-testid="flashes">
            <% flash.each do |message_type, message| %>
              <div class="toast toast-<%= message_type %>"><%= message %></div>
            <% end %>
          </div>
        </div>
        <%= yield %>
      </div>
    </div>
    </main>
    <footer>
    <%= debug(params) if Rails.env.development? %>
    </footer>
  </body>
</html>
